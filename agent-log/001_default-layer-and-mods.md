## ステータス
- Created: `2026-04-16 16:40:53`
- Status: `Done`

## 初回依頼
- Date: `2026-04-16 16:40:53`

デフォルトレイヤーの文字・記号配置を入れ替え、コンボ入力だった記号の一部を単打キーに移動する要望。  
合わせて Homerow Mods の割り当てを変更し、Command 誤爆を減らすため hold-tap の判定時間を長くする要望。

## 実装01
- Date: `2026-04-16 16:40:53`
- Model: `gpt-5.3-codex`

### 実装内容
config/cornix.keymap を更新。  
デフォルトレイヤーで次を変更した:
- 左上外列を Q、右上外列を P に変更
- 左下段で Z を X の左へ移動
- 左下段の V と B を入れ替え
- 右下段の P/Q 位置を 、/。 に変更
- 右下段の Q 右隣に ? を追加
- 右ホーム外列を Z から - に変更

Homerow Mods を次のように変更した:
- A を Ctrl から Shift に変更
- F を通常キーから Ctrl ホールドに変更
- J を通常キーから Ctrl ホールドに変更
- 右ホーム外列を Ctrl から Shift ホールドに変更し、タップは - に設定
- Command と Option の割り当ては維持

コンボ定義は次を削除し、単打キーへの移行を反映した:
- JK の 、コンボ
- KL の 。コンボ
- UI の ? コンボ
- JL の - コンボ（BASE/SYM/NAV）

誤爆対策として hold-tap 判定時間を変更した:
- HM_TAPPING_TERM を 250 から 350 に変更
- HM_PRIOR_IDLE は 150 のまま維持

just / west などのビルドコマンドを試したが、実行環境に just と west がなくビルド確認は実行できなかった。

### 確認項目とフィードバック
1. JL の - コンボを残したい。あわせて 、と。のコンボも残したい
2. 右側の Shift は変更後の - キー長押しで認識できていれば意図どおり
3. かって の誤爆はまだ気になる。K を押してすぐ A では Command+A にならず、K を少し待ってから A でだけ Command+A になる挙動を希望

## 実装02
- Date: `2026-04-16 16:47:58`
- Model: `gpt-5.3-codex`

### 実装内容
config/cornix.keymap を追加修正。  
要望どおり、次のコンボを復活させた:
- JK の 、コンボ（BASE/SYM/NAV）
- KL の 。コンボ
- JL の - コンボ（BASE/SYM/NAV）

右側 Shift については、現在の設定が `&hm_r RSHFT MINUS` なので、右ホーム外側キーはタップで -、長押しで Shift のまま維持されている。

K を押してすぐ A を重ねたときの Command+A 誤爆を減らすため、Homerow Mods の判定方針を変更した:
- hm_l / hm_r の flavor を `balanced` から `tap-preferred` に変更
- HM_TAPPING_TERM=350、HM_PRIOR_IDLE=150 は維持

この変更により、同時気味の打鍵では文字入力側が優先されやすくなり、修飾キーとして使うには少し待って長押しする必要が出る設定になった。

just / west が環境にないため、ビルド実行での確認は今回もできなかった。

### 確認項目とフィードバック
1. JK/KL/JL のコンボ復活が意図どおりか
2. 右ホーム外側キーがタップで -、長押しで Shift になっているか
3. かって の入力で Command+A 誤爆が減ったか

## 完了
- Date: `2026-04-16 16:53:13`

デフォルトレイヤーの再配置、記号キーの単打化と主要コンボの共存、Homerow Mods の配置変更、誤爆対策の hold-tap 調整を完了。  
右側ホーム外側キーはタップで -、長押しで Shift の設定で確定。
